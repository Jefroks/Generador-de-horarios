name: local_schedule_planner
version: 1.0.0+1
publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  http: ^1.2.2

flutter:
  uses-material-design: true
  assets:
    - assets/data/professors_seed.json
